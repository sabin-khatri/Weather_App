import 'package:flutter/material.dart';

class MyHomePage extends StatelessWidget {
  const MyHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    // return const Placeholder(
    //   color: Colors.blueGrey,
    //   strokeWidth: 24,
      
    // );
    // return Scaffold(
    //   appBar: AppBar(title: const Text('My App Bar'), backgroundColor: Colors.deepPurple,), 
    //   body: const Center(child: Text('You have pushed the button this many times:')),
    //   floatingActionButton: Center(
    //     child: FloatingActionButton(onPressed: () {}, tooltip: 'add', child: const Icon(Icons.add)), 
        

        
    //   ),

    // );

    return SafeArea(child: AppBar(

    )); 
  }
}